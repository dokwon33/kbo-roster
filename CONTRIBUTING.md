# 기여 가이드

이 프로젝트는 MIT 라이선스로 공개되어 있고, 누구나 자유롭게 PR을 보낼 수 있습니다.

## 브랜치 전략

`main`(배포용) + `develop`(통합) + `feature/*`(작업 단위)의 간소화된 Git Flow를 사용합니다.

1. 저장소를 포크한다.
2. `develop`에서 `feature/작업-이름` 브랜치를 만든다.
3. 작업 후 본인 포크의 같은 브랜치에 push한다.
4. 이 저장소의 `develop` 브랜치를 대상으로 PR을 연다 (`main`으로 직접 PR은 지양).

## PR 올리기 전 체크리스트

- [ ] `python manage.py check` 통과
- [ ] 새 기능이면 로컬에서 실제로 실행해보고 동작 확인 (`python manage.py runserver`)
- [ ] 민감 정보(API 키 등)는 `.env`에만 두고 커밋하지 않았는지 확인
- [ ] 커밋 메시지에 "무엇을" 보다 "왜" 바꿨는지 설명 포함

## 로컬 개발 환경

README의 "로컬 실행" 섹션을 따라 세팅한다. 뉴스/AI 요약 기능은 네이버 뉴스 검색 API와
Groq API 키가 있어야 동작하며, 없어도 다른 기능은 정상 동작한다.

## 이슈 / 아이디어 제안

버그 제보나 기능 제안은 GitHub Issues에 등록해주세요. 큰 기능(예: 새로운 데이터 소스 연동)을
추가하려는 경우, 구현 전에 이슈로 먼저 논의하는 것을 권장합니다.
